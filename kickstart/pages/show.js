// Each such file in pages directory will be a react component

import React from 'react';

// For the following we say exporting a functional component

export default () => {
  return(
    <h1>This is the show.js page</h1>
  );
};
